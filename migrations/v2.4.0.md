# Migration — v2.4.0

Applied by `migrate-project` to projects whose `agents/.plugin-version` records `2.3.0` or earlier.

## Project-side deltas

### Files added
(none)

### Files modified

- **`CLAUDE.md` (root)** — add rule #8 under the `## Non-negotiable rules` block.

  Diff base: the v2.3.0 root CLAUDE.md template ends at rule #7 (`If Node project: read agents/TOOLING.md…`). Add the following as rule #8 immediately after rule #7:

  ```
  8. **UI work uses the design system. No hardcoded values, ever.** Before editing any `.tsx` / `.jsx` / `.vue` / `.svelte` / `.swift` / styles file: read `agents/DESIGN.md` for tokens and `agents/FUNDAMENTALS.md` for the Token Rule. Run the `design-check` skill before writing and again after. Raw hex, raw px, raw `font-family` strings, emoji-as-icon, and other cardinal sins from `FUNDAMENTALS.md` in component files are violations — not preferences. If a needed token doesn't exist in `DESIGN.md`: stop and propose adding it. Do not improvise.
  ```

  Detection: if the root CLAUDE.md already contains the string `UI work uses the design system`, this delta is already present — skip it.

### Files renamed / removed
(none)

---

## Previous template snapshot reference

Template as of v2.3.0: `skills/init-project/references/phase-3-three-folder-create.md` at git SHA of the v2.3.0 release commit (tag `v2.3.0` if available, otherwise the commit immediately before the v2.4.0 bump).

To diff the project's current root CLAUDE.md against the v2.3.0 template base:

```bash
git show v2.3.0:skills/init-project/references/phase-3-three-folder-create.md 2>/dev/null | grep -A 40 "Root \`CLAUDE.md\` template"
```
