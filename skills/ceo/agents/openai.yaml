interface:
  display_name: "CEO"
  short_description: "Orchestrate work as the boss who defines chapters, delegates to workers, and verifies results — for running the CEO/worker delegation loop"
  default_prompt: "Act as CEO: orient from live canon, define or verify chapters, review worker evidence and diffs selectively, and reconcile only approved work into the integration branch."

policy:
  allow_implicit_invocation: false
