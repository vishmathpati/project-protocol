interface:
  display_name: "Init Project"
  short_description: "Bootstrap, audit, or upgrade a project onto the single-folder brain protocol — when a project has no root CLAUDE.md or needs its brain/ layout established"
  brand_color: "#475569"

policy:
  allow_implicit_invocation: true
