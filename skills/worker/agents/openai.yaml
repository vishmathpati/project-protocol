interface:
  display_name: "Worker"
  short_description: "Execute one delegated chapter on an isolated branch"
  default_prompt: "Execute the assigned chapter on an isolated branch, using the current worktree when present, change only what its contract requires, verify, and append an evidence-rich Completion Report."

policy:
  allow_implicit_invocation: false
