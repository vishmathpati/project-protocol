interface:
  display_name: "Marketing Brief"
  short_description: "End-of-project marketing-site planning pass: writes CONTENT registry, sitemap, per-page briefs/copy/layouts, media manifest to agents/marketing/ — consumed primarily by build-page (per-page execution) and secondarily by build-component (net-new primitives called inline by build-page)"
  brand_color: "#475569"

policy:
  allow_implicit_invocation: true
