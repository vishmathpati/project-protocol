interface:
  display_name: "Project Dashboard"
  short_description: "Generate the human-readable project dashboard"
  default_prompt: "Generate or check a concise visual Project Dashboard: summarize canon, show one research concept/site at a time with large grouped evidence and live links, and keep review notes browser-local rather than creating a second source of truth."

policy:
  allow_implicit_invocation: false
