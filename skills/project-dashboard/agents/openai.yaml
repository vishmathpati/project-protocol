interface:
  display_name: "Project Dashboard"
  short_description: "Review and submit provisional visual decisions"
  default_prompt: "Generate or serve the active worktree's Project Dashboard, present page-aware recommendations and evidence for site-wide human review, and keep submitted decisions provisional until Claude or Codex validates and records approved Markdown canon."

policy:
  allow_implicit_invocation: false
