interface:
  display_name: "Design Check"
  short_description: "Keep visual changes aligned with the design system"
  default_prompt: "Use $design-check for this exact UI scope, including applicable site/page compatibility, existing tokens/components, changed hunks, media gates, and rendered evidence."

policy:
  allow_implicit_invocation: true
