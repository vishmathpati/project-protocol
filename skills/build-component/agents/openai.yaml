interface:
  display_name: "Build Component"
  short_description: "Build a genuinely missing on-system UI component"
  default_prompt: "Use $build-component to search for reuse first and build only the missing on-system component, requiring an active approved page for research-led marketing work."

policy:
  allow_implicit_invocation: true
