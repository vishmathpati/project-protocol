interface:
  display_name: "Build Component"
  short_description: "5-phase, tier-aware (Generic/Marketing/App), surface-aware build for a new component — reuse first, normalize to DESIGN.md tokens, write to STRUCTURE.md location, then chain into design-check."
  brand_color: "#475569"

policy:
  allow_implicit_invocation: true
