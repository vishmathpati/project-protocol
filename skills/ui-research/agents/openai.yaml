interface:
  display_name: "UI Research"
  short_description: "Discover, select, then inspect evidence-backed UI concepts"
  default_prompt: "Respect the target, worktree, and reference scope; distinguish open discovery, pinned-site concept discovery, and explicitly selected teardown; generate the dashboard for human selection before any deep teardown."

policy:
  allow_implicit_invocation: false

dependencies:
  tools:
    - type: "mcp"
      value: "aside"
  references:
    - "aside-skill/ui-research/SKILL.md"
    - "references/mission-prompt-template.md"
    - "references/round-formats.md"
