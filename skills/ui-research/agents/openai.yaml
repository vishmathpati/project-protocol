interface:
  display_name: "UI Research"
  short_description: "Discover UI concepts or inspect pinned references"
  default_prompt: "Respect the user's target, current worktree, and reference scope; prepare an evidence-integrity Aside mission; then ingest honestly classified research and moodboard evidence."

policy:
  allow_implicit_invocation: false

dependencies:
  tools:
    - type: "mcp"
      value: "aside"
  references:
    - "aside-skill/ui-research/SKILL.md"
    - "references/mission-prompt-template.md"
    - "references/round-formats.md"
