interface:
  display_name: "UI Research"
  short_description: "Discover, select, then inspect evidence-backed UI concepts"
  default_prompt: "Respect target, worktree, and reference scope; enforce human concept selection; require settled fonts/images/video/layout before complete captures; and generate a one-at-a-time visual dashboard before any selected teardown."

policy:
  allow_implicit_invocation: false

dependencies:
  tools:
    - type: "mcp"
      value: "aside"
  references:
    - "aside-skill/ui-research/SKILL.md"
    - "references/mission-prompt-template.md"
    - "references/round-formats.md"
