interface:
  display_name: "Save Session"
  short_description: "Save and close a project session, persisting the session's work into canon — for the end of a working session"
  brand_color: "#475569"

policy:
  allow_implicit_invocation: true
