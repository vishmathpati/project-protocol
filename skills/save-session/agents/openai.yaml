interface:
  display_name: "Save Session"
  short_description: "Persist role-owned session state safely and report Git outcome"
  default_prompt: "Save this session according to the invoked role, fold durable state into its proper canon, commit explicit owned files, and report the exact next action."

policy:
  allow_implicit_invocation: false
