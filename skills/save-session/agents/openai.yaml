interface:
  display_name: "Save Session"
  short_description: "Close a session: brain/WORKLOG to CHANGELOG, update STATUS/BRIEF, clear WORKLOG (single brain/ canon)"
  brand_color: "#475569"

policy:
  allow_implicit_invocation: true
