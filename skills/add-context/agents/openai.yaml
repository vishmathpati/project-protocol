interface:
  display_name: "Add Context"
  short_description: "Add and index extended project context"
  default_prompt: "Add the requested context under brain/docs, then register it in brain/docs/INDEX.md and add only a concise trigger pointer to root CLAUDE.md."
  brand_color: "#475569"

policy:
  allow_implicit_invocation: true
