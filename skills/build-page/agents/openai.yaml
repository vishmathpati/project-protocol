interface:
  display_name: "Build Page"
  short_description: "Compose and verify a substantial brand-facing page"
  default_prompt: "Compose this substantial page content-first: lock section jobs and narrative order, map the human-selected concept, offer grounded presentation choices, then make every media slot and replacement gate explicit before implementation."

policy:
  allow_implicit_invocation: true
