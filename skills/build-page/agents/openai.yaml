interface:
  display_name: "Build Page"
  short_description: "Compose and verify a substantial brand-facing page"
  default_prompt: "Compose this substantial page from locked content, design canon, explicitly selected optional research, and existing components; stop at the UI Research checkpoint when research exists without human selection."

policy:
  allow_implicit_invocation: true
