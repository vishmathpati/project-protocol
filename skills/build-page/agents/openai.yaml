interface:
  display_name: "Build Page"
  short_description: "Compose and verify a substantial brand-facing page"
  default_prompt: "Use $build-page to verify the locked site direction and active-page compatibility, preserve connected composition, and build content-first with an explicit Page Asset Request."

policy:
  allow_implicit_invocation: true
