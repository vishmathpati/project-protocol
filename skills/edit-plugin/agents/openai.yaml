interface:
  display_name: "Edit Plugin"
  short_description: "Mandatory discipline gate whenever this plugin's own source (skills, hooks, templates, manifest) is being changed"
  brand_color: "#7C2D12"

policy:
  allow_implicit_invocation: true
