interface:
  display_name: "Solo"
  short_description: "Both hats for small work: plan lightly, do it yourself, write a short Completion Report (chapter or WORKLOG), commit — no worktree, no handoff"
  brand_color: "#6d28d9"

policy:
  allow_implicit_invocation: false
